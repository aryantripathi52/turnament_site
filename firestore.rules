rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // THE ULTIMATE DEBUG RULE: Allow everything for anyone logged in
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
