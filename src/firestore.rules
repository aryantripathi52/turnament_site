rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- Global Admin Overrides ---
    match /addCoinRequests/{id} {
      allow read, write, list: if request.auth != null && request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2';
    }
    match /withdrawCoinRequests/{id} {
      allow read, write, list: if request.auth != null && request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2';
    }
    match /users/{userId} {
      allow read, write, list: if request.auth != null && (request.auth.uid == userId || request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2');
    }

    // --- Player-Specific Subcollections ---
    match /users/{userId}/{document=**} {
      allow read, write, list: if request.auth != null && request.auth.uid == userId;
    }

    // --- Tournament Rules ---
    match /tournaments/{tournamentId} {
      allow read: if true;
      allow create, delete: if request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2';
      
      allow update: if (request.auth != null && 
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['registeredCount'])) || request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2';

      match /registrations/{registrationId} {
        allow read, list: if request.auth != null;
        // Players can only create registrations for themselves
        allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      }
    }
    
    // --- Game Category Rules ---
    match /categories/{categoryId} {
        allow read: if true;
        allow write: if request.auth.uid == 'QNH804sx9uO9KpGQYUfQ9BU6CKF2';
    }

    // --- Public, Read-Only Collections ---
    match /announcements/{announcementId} {
      allow read: if true;
    }
     match /highlights/{highlightId} {
      allow read: if true;
    }
  }
}
