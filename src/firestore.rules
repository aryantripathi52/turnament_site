rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- GLOBAL SUPER ADMIN OVERRIDE ---
    // This rule grants total access to a single, hardcoded UID. It is checked first.
    // PASTE YOUR ADMIN UID FROM THE FIREBASE AUTHENTICATION TAB HERE
    match /{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == 'YOUR_ACTUAL_UID_HERE';
    }

    // --- REGULAR USER & PLAYER RULES ---

    // Rule: User profiles
    match /users/{userId} {
      // A user can create their own profile upon registration.
      allow create: if request.auth != null && request.auth.uid == userId;
      // A user can read/update their own profile.
      allow read, update: if request.auth != null && request.auth.uid == userId;

      // Rules for user sub-collections
      match /joinedTournaments/{tournamentId} {
        allow read, write, list: if request.auth != null && request.auth.uid == userId;
      }
       match /wonTournaments/{tournamentId} {
        allow read, list: if request.auth != null && request.auth.uid == userId;
      }
       match /addCoinRequests/{requestId} {
        allow read, write, list: if request.auth != null && request.auth.uid == userId;
      }
       match /withdrawCoinRequests/{requestId} {
        allow read, write, list: if request.auth != null && request.auth.uid == userId;
      }
    }

    // Rule: Tournaments
    match /tournaments/{tournamentId} {
      allow read: if true;
      
      // Players can only update 'registeredCount' when joining.
      allow update: if request.auth != null && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['registeredCount']);

      match /registrations/{registrationId} {
        allow read, list: if request.auth != null;
        allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      }
    }
    
    // Rule: Game Categories (Publicly readable)
    match /categories/{categoryId} {
        allow read: if true;
    }

    // --- GLOBAL REQUEST COLLECTIONS ---
    
    match /addCoinRequests/{requestId} {
        // Players can create their own requests.
        allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    }
    
    match /withdrawCoinRequests/{requestId} {
        // Players can create their own requests.
        allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    }

    // Rule: Public, read-only collections
    match /announcements/{announcementId} {
      allow read: if true;
    }
     match /highlights/{highlightId} {
      allow read: if true;
    }
  }
}
